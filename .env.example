# DocTrust environment template — VARIABLE NAMES ONLY, never values.
# Copy to .env and fill in real values locally; .env is gitignored and must
# never be tracked (Phase-5 remediation F-2).

# Nutrient DWS credentials
extraction_apikey=
processor_apikey=
accessibilty_apikey=
NUTRIENT_DWS_EXTRACTION_API_KEY=
NUTRIENT_EXTRACTION_KEY=
NUTRIENT_PROCESSOR_KEY=

# LLM authoring (candidate check authoring only; never used at runtime)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# Campaign credentials
username=
password=
